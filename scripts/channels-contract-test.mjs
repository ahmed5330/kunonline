import {readFile} from 'node:fs/promises';
const migration=await readFile(new URL('../migrations/0006_channels_campaigns.sql',import.meta.url),'utf8');
const worker=await readFile(new URL('../src/index-commerce-v8.js',import.meta.url),'utf8');
const access=await readFile(new URL('../src/access-control.js',import.meta.url),'utf8');
const collabMigration=await readFile(new URL('../migrations/0091_internal_collaboration.sql',import.meta.url),'utf8');
const collab=await readFile(new URL('../src/internal-collaboration-v117.js',import.meta.url),'utf8');
const collabOrderInput=await readFile(new URL('../src/internal-collaboration-order-input-v119.js',import.meta.url),'utf8');
const entry=await readFile(new URL('../src/index-commerce-v38.js',import.meta.url),'utf8');
const nav=await readFile(new URL('../public/v2/modules-v51-permission-navigation.js',import.meta.url),'utf8');
const collabSidebar=await readFile(new URL('../public/v2/modules-v117-collaboration-sidebar.js',import.meta.url),'utf8');
const collabOrderLink=await readFile(new URL('../public/v2/modules-v119-collaboration-order-link.js',import.meta.url),'utf8');
const staticShellFallback=await readFile(new URL('../public/v2/modules-v108-returns-view-race-guard.js',import.meta.url),'utf8');
const customerServiceNavReady=await readFile(new URL('../public/v2/modules-v118-customer-service-nav-ready.js',import.meta.url),'utf8');
const mobileRunner=await readFile(new URL('./browser-preview-mobile-qa-runner.mjs',import.meta.url),'utf8');
const must=(ok,msg)=>{if(!ok)throw new Error(msg)};
for(const t of ['conversations','channel_messages','marketing_campaigns','campaign_daily_metrics'])must(migration.includes(`CREATE TABLE IF NOT EXISTS ${t}`),`Missing ${t}`);
for(const e of ['/api/inbox/conversations','/api/campaigns','/api/campaigns/summary'])must(worker.includes(e),`Missing ${e}`);
must(worker.includes("status:'queued'"),'Outbound inbox messages must queue instead of pretending to send');
must(worker.includes("method==='POST')return createCampaign"),'Campaign creation route missing');
must(worker.includes('DUPLICATE_CAMPAIGN'),'Campaign creation must prevent duplicate platform/name pairs');
must(worker.includes("'campaign.create'"),'Campaign creation must write an audit event');
must(access.includes("'inbox.*'"),'Inbox permissions missing');
must(access.includes("'campaigns.*'"),'Campaign permissions missing');
for(const t of ['collab_channels','collab_channel_members','collab_messages','collab_message_mentions','collab_channel_reads','collab_tasks','collab_order_assignments'])must(collabMigration.includes(`CREATE TABLE IF NOT EXISTS ${t}`),`Missing collaboration table ${t}`);
must(collab.includes('resolveTenant(me,requestedClient)'),'Collaboration must preserve tenant isolation');
must(collab.includes('resolveStoreScope(env,me,clientId,requestedStore,{write})'),'Collaboration must preserve store isolation and viewer write guards');
must(collab.includes('WHERE id=? AND client_id=? AND store_id=?'),'Canonical collaboration order reads must stay inside the active client/store');
must(entry.includes("requirePermission(me,'inbox',request.method.toUpperCase()==='GET'?'read':'write')"),'Collaboration API must enforce Inbox read/write permissions');
must(entry.includes('handleCollaborationOrderInputV119')&&entry.includes('collaborationOrderInput?.request||request'),'Collaboration order input normalization must run before the v117 collaboration handler');
must(entry.includes('modules-v117-collaboration-sidebar.js?v=117.3')&&entry.includes('data-kun-collaboration-sidebar="1"'),'Preview v2 shell must load the first-class collaboration sidebar route after collaboration');
must(entry.includes('modules-v119-collaboration-order-link.js?v=119.0')&&entry.includes('data-kun-collaboration-order-link="1"'),'Preview v2 shell must load searchable collaboration order linking');
must(entry.includes('modules-v118-customer-service-nav-ready.js?v=118.0')&&entry.includes('data-kun-customer-service-nav-ready="1"'),'Preview v2 shell must preserve Customer Service clicks that land before v31 finishes booting');
must(entry.includes('function missingV2UiScripts(html)')&&entry.includes('V2_UI_SCRIPTS.filter(tag=>'),'Preview v2 UI injection must check every dynamic module independently');
must(!entry.includes("if(html.includes('data-kun-customer-service-claim=\"1\"'))return"),'One legacy marker must never suppress newer collaboration/mobile UI injection');
must(entry.includes('const html=await response.text(),scripts=missingV2UiScripts(html)'),'Preview HTML must inject only the missing v2 runtime modules');
must(collabOrderInput.includes("replace(/^#\\s*/,'')"),'Collaboration order linking must accept a leading # without breaking lookup');
must(collabOrderInput.includes('(id=? OR ref=? OR awb=?)'),'Collaboration order linking must resolve internal ID, Easy Orders/reference code and AWB');
must(collabOrderInput.includes('resolveStoreScope(env,me,clientId,requestedStore,{write})'),'Order lookup/search must preserve active-store isolation');
must(collabOrderInput.includes("path==='/api/collaboration/orders/search'")&&collabOrderInput.includes("COALESCE(phone,'') LIKE ?")&&collabOrderInput.includes("COALESCE(name,'') LIKE ?"),'Collaboration order picker must offer scoped search by code, AWB, phone and customer');
must(collabOrderInput.includes('body.orderId=order.id'),'Message/task order tokens must be canonicalized before v117 persists them');
must(collabOrderLink.includes("placeholder='رقم الأوردر / كود Easy Orders / AWB'")&&collabOrderLink.includes('/api/collaboration/orders/search'),'Collaboration composer must expose searchable ID/ref/AWB order linking');
must(collabOrderLink.includes('window.kunStoreId')&&collabOrderLink.includes('الفرع الحالي فقط'),'Order picker must remain explicitly scoped to the selected store');
must(nav.includes("collaboration:['inbox.read']"),'Dynamic collaboration navigation must inherit Inbox read permission');
must(nav.includes('refreshLateInjectedNavigation')&&nav.includes('setTimeout(refreshLateInjectedNavigation,650)'),'Permission navigation must refresh only the late-injected collaboration item without re-applying every navigation rule');
must(!nav.includes('setTimeout(()=>{if(ready)apply();},650)'),'Late collaboration injection must not trigger a full navigation re-apply');
must(!nav.includes('new MutationObserver'),'Permission navigation must not watch every navigation mutation');
must(nav.includes("permissionNavigationVersion='51.11'"),'Permission navigation must expose the active runtime version marker');
must(collabSidebar.includes("button[data-view=\"collaboration\"]")&&collabSidebar.includes("classList.add('nav-standalone','nav-collaboration')"),'Collaboration must be promoted to a visible standalone sidebar route');
must(collabSidebar.includes('[data-kun-shortcuts-nav]')&&collabSidebar.includes("dataset.collaborationSidebar='ready'"),'Collaboration sidebar placement must stay visible beside the primary mobile navigation and expose a readiness marker');
must(collabSidebar.includes("dataset.collaborationStandalone=VERSION"),'Collaboration sidebar placement must mark its one-time normalization state');
must(!collabSidebar.includes('KunSidebarGroupsV90?.sync?.()'),'Collaboration sidebar placement must not broadly re-sync grouped navigation and race unrelated views');
must(staticShellFallback.includes('ensureCollaborationAssets')&&staticShellFallback.includes("window.addEventListener('load',ensureCollaborationAssets"),'Static /v2 asset delivery must bootstrap collaboration after the shell finishes loading');
must(staticShellFallback.includes('/v2/modules-v117-collaboration.js?v=117.0')&&staticShellFallback.includes('/v2/modules-v117-collaboration-sidebar.js?v=117.3')&&staticShellFallback.includes('/v2/modules-v119-collaboration-order-link.js?v=119.0'),'Static shell fallback must load collaboration, sidebar and searchable order-link assets');
must(staticShellFallback.includes("dataset.collaborationAssetsFallback='ready'"),'Static shell fallback must expose a collaboration readiness marker');
must(customerServiceNavReady.includes('customerServiceNavReady=\'pending\'')&&customerServiceNavReady.includes('KunCustomerServiceV31.render()'),'Early Customer Service navigation must be replayed exactly through the v31 renderer once that route is ready');
must(customerServiceNavReady.includes("button.classList.contains('is-visible')")&&customerServiceNavReady.includes('attempts<80'),'Customer Service early-click recovery must be bounded and wait for the real v31 readiness marker');
must(mobileRunner.includes('fresh mobile shell before'),'Mobile exhaustive QA must isolate its 390px and 360px viewport sweeps');
must(mobileRunner.includes('collaboration standalone sidebar route'),'Mobile browser QA must explicitly guard the visible collaboration sidebar route');
console.log('Channels contract checks passed: unified inbox, campaign analytics, deterministic collaboration UI bootstrap and store-scoped searchable order linking.');