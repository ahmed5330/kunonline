import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';
import {buildJtCreatePayload,createJtShipment,jtWebhookToken,__jtApiInternals} from '../src/jt-express-eg-api.js';
import {__jtValidationInternals} from '../src/jt-express-eg-validation.js';

const basicSecrets={api_account:'API-123',private_key:'PRIVATE-XYZ',source_code:'D452'};
const secrets={...basicSecrets,customer_code:'J0088',customer_password:'secret',sender_name:'Kun Warehouse',sender_mobile:'01000000000',sender_phone:'0220000000',sender_company:'Kun Online',sender_prov:'القاهرة',sender_city:'مدينة نصر',sender_area:'الحي السابع',sender_street:'شارع الاختبار 1'};
const shipment={orderId:'OID-1',customerOrderNo:'ORD-1',receiverName:'Ahmed Test',receiverPhone:'01012345678',countryCode:'+20',province:'القاهرة',provinceCode:'1011',city:'مدينة نصر',cityCode:'1011001',area:'الحي السابع',districtCode:'A000001',addressCountryCode:'100000',street:'شارع الاختبار',itemType:'Clothes',itemName:'Skirt',weight:1.25,quantity:2,codAmount:1200,currency:'EGP'};
assert.throws(()=>buildJtCreatePayload(shipment,basicSecrets),error=>error?.code==='JT_ENTERPRISE_INFO_REQUIRED'&&error?.status===409,'Create Order must fail clearly before calling J&T when Enterprise Info is missing');
assert.throws(()=>buildJtCreatePayload(shipment,{...basicSecrets,customer_code:'J0088',customer_password:'secret'}),error=>error?.code==='JT_SENDER_INFO_MISSING'&&error?.status===409,'Create Order must fail clearly before calling J&T when sender info is missing');
const payload=buildJtCreatePayload(shipment,secrets);
assert.equal(payload.sourceCode,'D452');assert.equal(payload.customerCode,'J0088');assert.ok(payload.digest);assert.equal(payload.txlogisticId,'ORD-1');assert.equal(payload.serviceType,'01');assert.equal(payload.deliveryType,'04');assert.equal(payload.receiver.prov,'القاهرة');assert.equal(payload.receiver.provCode,'1011');assert.equal(payload.receiver.cityCode,'1011001');assert.equal(payload.receiver.areaCode,'A000001');assert.equal(payload.receiver.countryCode,'EGY');assert.equal(payload.sender.name,'Kun Warehouse');assert.equal(payload.sender.mobile,'01000000000');assert.equal(payload.sender.prov,'القاهرة');assert.equal(payload.sender.city,'مدينة نصر');assert.equal(payload.sender.area,'الحي السابع');assert.equal(payload.sender.street,'شارع الاختبار 1');assert.equal(payload.payType,'PP_CASH');assert.equal(payload.itemsValue,1200);assert.equal(payload.totalQuantity,2);
const calls=[];
const result=await createJtShipment({shipment,secrets,fetcher:async(url,options)=>{calls.push({url,options});assert.equal(url,`${__jtApiInternals.LIVE_BASE}${__jtApiInternals.ADD_ORDER_PATH}`);assert.equal(options.method,'POST');assert.equal(options.headers.apiAccount,secrets.api_account);assert.match(options.headers.timestamp,/^\d{10}$/);const bizContent=new URLSearchParams(options.body).get('bizContent');assert.ok(bizContent);assert.equal(options.headers.digest,__jtValidationInternals.md5Base64(bizContent+secrets.private_key));const sent=JSON.parse(bizContent);assert.equal(sent.sourceCode,'D452');assert.equal(sent.customerCode,'J0088');assert.ok(sent.digest);assert.equal(sent.receiver.areaCode,'A000001');assert.equal(sent.sender.name,'Kun Warehouse');assert.equal(sent.serviceType,'01');return new Response(JSON.stringify({code:'1',msg:'success',data:{billCode:'JT123456789EG',txlogisticId:'ORD-1',sortingCode:'20 C01-03'}}),{status:200,headers:{'Content-Type':'application/json'}});}});
assert.equal(calls.length,1);assert.equal(result.awb,'JT123456789EG');assert.equal(result.sortingCode,'20 C01-03');assert.equal(result.txlogisticId,'ORD-1');
assert.equal(__jtApiInternals.parseJtShipmentResult({data:{bill_code:'JT-BILL-2'}}).awb,'JT-BILL-2');assert.equal(__jtApiInternals.parseJtShipmentResult({data:{awbNo:'JT-BILL-3'}}).awb,'JT-BILL-3');
const token1=jtWebhookToken('CON-1',basicSecrets),token2=jtWebhookToken('CON-2',basicSecrets);assert.ok(token1.length>10);assert.notEqual(token1,token2);
const edit=await readFile(new URL('../src/order-edit.js',import.meta.url),'utf8'),worker=await readFile(new URL('../src/index-commerce-v37.js',import.meta.url),'utf8'),ui=await readFile(new URL('../public/v2/modules-v78-jt-shipping-order.js',import.meta.url),'utf8'),setup=await readFile(new URL('../public/v2/modules-v81-jt-live-setup.js',import.meta.url),'utf8'),trackingUi=await readFile(new URL('../public/v2/modules-v82-jt-tracking-cards.js',import.meta.url),'utf8'),recencyUi=await readFile(new URL('../public/v2/modules-v83-order-recency.js',import.meta.url),'utf8'),createSetupUi=await readFile(new URL('../public/v2/modules-v84-jt-create-setup.js',import.meta.url),'utf8'),index=await readFile(new URL('../public/v2/index.html',import.meta.url),'utf8');
new Function(ui);new Function(setup);new Function(trackingUi);new Function(recencyUi);new Function(createSetupUi);
assert.match(edit,/EDITABLE_STATES=new Set\(\['pending','no_answer','confirmed','preparing','deferred'\]\)/,'no_answer must remain editable before confirmation');
for(const marker of ['function extractTracking','function delivered','jt_tracking_update','constantTimeEqual','/api/webhooks/jt/logistics/','/api/jt/webhook-url','nextState=\'signed\''])assert.ok(worker.includes(marker),`J&T webhook worker missing ${marker}`);
assert.ok(ui.includes('/api/jt/shipments/'),'J&T form must call the live server-side shipment API');assert.ok(!ui.includes('KunCustomerServiceV31?.render?.()'),'J&T shipment must not reload the Customer Service board');assert.ok(ui.includes("moveState?.(orderId,'shipped')"),'Successful J&T creation must move only the order card');
assert.ok(setup.includes('/api/jt/webhook-url'),'J&T setup must expose the logistics callback URL');assert.ok(setup.includes('data-secret="customer_code"')&&setup.includes('data-secret="customer_password"'),'Enterprise Info inputs must be available in the J&T setup');
for(const marker of ['sender_name','sender_mobile','sender_prov','sender_city','sender_area','sender_street','Bill Code'])assert.ok(createSetupUi.includes(marker),`J&T Create Order setup missing ${marker}`);
for(const marker of ['/api/customer-service','/api/post-shipping','/api/returns-exchanges','history.at(-1)','patchOrder','touch(orderId)'])assert.ok(recencyUi.includes(marker),`Operational recency layer missing ${marker}`);
assert.ok(index.includes('/v2/modules-v81-jt-live-setup.js?v=81.0'),'J&T webhook setup module must be loaded by the app');assert.ok(index.includes('/v2/modules-v82-jt-tracking-cards.js?v=82.0'),'J&T tracking cards module must be loaded by the app');assert.ok(index.includes('/v2/modules-v83-order-recency.js?v=83.0'),'Operational recency module must be loaded by the app');assert.ok(index.includes('/v2/modules-v84-jt-create-setup.js?v=84.0'),'Complete J&T Create Order setup module must be loaded by the app');
for(const marker of ['post-shipping','returns-exchanges','jt82-badge','jt_tracking_update','jt_shipment_created','60000'])assert.ok(trackingUi.includes(marker),`J&T tracking UI missing ${marker}`);
assert.ok(trackingUi.includes("window.KunPostShippingV47?.render?.()"),'Post-shipping board must re-render when a J&T webhook moves an order to another stage');
console.log('J&T live shipping checks passed: complete Enterprise/sender Create Order payload, Bill Code parsing, clear preflight failures, operational recency, webhook routes, no board reload, and live tracking cards.');
