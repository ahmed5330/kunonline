/* kun online v17 — multi-store + AI insights */
(function(){
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  async function api(path,options={}){const r=await fetch(path,{credentials:'include',headers:{'Content-Type':'application/json',...(options.headers||{})},...options});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d;}
  const sev=s=>`<span class="badge ${s==='danger'?'b-cancelled':s==='warn'?'b-pending':'b-delivered'}">${esc(s||'info')}</span>`;
  async function renderStores(root){root.innerHTML=`<div class="page-head"><div><div class="title">المتاجر والفروع</div><div class="sub">إدارة أكثر من متجر أو فرع داخل نفس حساب Kun Online.</div></div><div class="spacer"></div><button class="btn primary" id="storeAdd">+ متجر جديد</button></div><div class="card"><div id="storesBody" class="empty">جارٍ التحميل...</div></div>`;const load=async()=>{const box=root.querySelector('#storesBody');try{const rows=await api('/api/stores');box.className=rows.length?'table-wrap':'empty';box.innerHTML=rows.length?`<table class="table compact"><thead><tr><th>المتجر</th><th>الكود</th><th>العملة</th><th>المنطقة الزمنية</th><th>الحالة</th><th>افتراضي</th></tr></thead><tbody>${rows.map(x=>`<tr><td><strong>${esc(x.name)}</strong></td><td>${esc(x.code||'—')}</td><td>${esc(x.currency||'EGP')}</td><td>${esc(x.timezone||'—')}</td><td>${esc(x.status)}</td><td>${x.is_default?'نعم':'—'}</td></tr>`).join('')}</tbody></table>`:'لم يتم إنشاء متاجر فرعية بعد.';}catch(e){box.className='empty';box.textContent=e.message;}};root.querySelector('#storeAdd').onclick=async()=>{const name=prompt('اسم المتجر أو الفرع');if(!name)return;try{await api('/api/stores',{method:'POST',body:JSON.stringify({name})});load();}catch(e){window.showToast?showToast(e.message):alert(e.message)}};load();}
  async function renderAi(root){root.innerHTML=`<div class="page-head"><div><div class="title">kun AI — مركز التوصيات</div><div class="sub">تحليل تشغيلي مبني على بيانات متجرك، مع فصل الاقتراح عن التنفيذ والموافقات الحساسة.</div></div><div class="spacer"></div><button class="btn primary" id="aiGenerate">تحليل الآن</button></div><div id="aiInsights" class="grid split"><div class="card">جارٍ التحميل...</div></div><div class="card mt"><div class="title" style="font-size:18px">سجل إجراءات AI</div><div id="aiActions" class="empty small-empty">جارٍ التحميل...</div></div>`;const load=async()=>{try{const [ins,actions]=await Promise.all([api('/api/ai/insights'),api('/api/ai-actions')]);const ib=root.querySelector('#aiInsights');ib.className=ins.length?'grid split':'empty';ib.innerHTML=ins.length?ins.map(x=>`<div class="card insight-card"><div class="page-head"><div><strong>${esc(x.title)}</strong><div class="meta">${esc(x.rationale||'')}</div></div>${sev(x.severity)}</div><div class="meta">${esc(x.insight_type)} · ${new Date(x.generated_at).toLocaleString('ar-EG')}</div><div class="toolbar mt"><button class="btn soft aiDismiss" data-id="${esc(x.id)}">إخفاء</button></div></div>`).join(''):'لا توجد توصيات نشطة. اضغط «تحليل الآن».';ib.querySelectorAll('.aiDismiss').forEach(b=>b.onclick=async()=>{await api(`/api/ai/insights/${encodeURIComponent(b.dataset.id)}/dismiss`,{method:'POST',body:'{}'});load();});const ab=root.querySelector('#aiActions');ab.className=actions.length?'table-wrap':'empty small-empty';ab.innerHTML=actions.length?`<table class="table compact"><thead><tr><th>الاقتراح</th><th>الإجراء</th><th>المخاطرة</th><th>الحالة</th></tr></thead><tbody>${actions.slice(0,50).map(x=>`<tr><td>${esc(x.title)}</td><td>${esc(x.action_type)}</td><td>${esc(x.risk)}</td><td>${esc(x.status)}</td></tr>`).join('')}</tbody></table>`:'لا توجد إجراءات AI مقترحة.';}catch(e){root.querySelector('#aiInsights').className='empty';root.querySelector('#aiInsights').textContent=e.message;}};root.querySelector('#aiGenerate').onclick=async()=>{try{await api('/api/ai/insights/generate',{method:'POST',body:'{}'});load();}catch(e){window.showToast?showToast(e.message):alert(e.message)}};load();}
  function hook(){document.addEventListener('click',e=>{const b=e.target.closest('.nav button');if(!b)return;const root=document.getElementById('root');if(b.dataset.view==='stores')setTimeout(()=>renderStores(root),0);if(b.dataset.view==='ai')setTimeout(()=>renderAi(root),0);});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',hook);else hook();
})();
