/* kun online v11 — SaaS control center */
(function(){
  const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  async function api(path,options={}){const r=await fetch(path,{credentials:'include',headers:{'Content-Type':'application/json',...(options.headers||{})},...options});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);return d;}
  const fmt=d=>d?new Date(d).toLocaleString('ar-EG'):'—';
  async function renderControl(root){root.innerHTML=`<div class="page-head"><div><div class="title">إدارة الحساب والتكاملات</div><div class="sub">الخطة والاستخدام والمتاجر المتصلة والدعم الفني.</div></div></div><div id="saasTop" class="grid kpis four"><div class="card">جارٍ التحميل...</div></div><div class="grid split mt"><div class="card"><div class="title" style="font-size:18px">المتاجر المتصلة</div><div id="saasConnections" class="empty small-empty">جارٍ التحميل...</div></div><div class="card"><div class="title" style="font-size:18px">الدعم الفني</div><div id="saasSupport" class="empty small-empty">جارٍ التحميل...</div></div></div>`;try{const [o,c,t]=await Promise.all([api('/api/tenant/overview'),api('/api/store-connections'),api('/api/support-tickets')]);const u=o.usage30d||{};root.querySelector('#saasTop').innerHTML=`<div class="card"><div class="k-label">الخطة</div><div class="k-val small">${esc(o.subscription?.plan||o.tenant?.plan||'trial')}</div></div><div class="card"><div class="k-label">طلبات 30 يوم</div><div class="k-val">${Number(u.orders_count||0)}</div></div><div class="card"><div class="k-label">AI Actions</div><div class="k-val">${Number(u.ai_actions||0)}</div></div><div class="card"><div class="k-label">Automation Runs</div><div class="k-val">${Number(u.automation_runs||0)}</div></div>`;const cb=root.querySelector('#saasConnections');cb.className=c.length?'table-wrap':'empty small-empty';cb.innerHTML=c.length?`<table class="table compact"><thead><tr><th>المنصة</th><th>المتجر</th><th>الحالة</th><th>آخر مزامنة</th><th>آخر خطأ</th></tr></thead><tbody>${c.map(x=>`<tr><td>${esc(x.provider)}</td><td>${esc(x.store_name||'—')}</td><td>${esc(x.status)}</td><td>${fmt(x.last_sync_at)}</td><td>${esc(x.last_error||'—')}</td></tr>`).join('')}</tbody></table>`:'لم يتم ربط متاجر بعد.';const tb=root.querySelector('#saasSupport');tb.className=t.length?'':'empty small-empty';tb.innerHTML=t.length?t.slice(0,10).map(x=>`<div class="insight info"><div><strong>${esc(x.subject)}</strong><div class="meta">${esc(x.status)} · ${esc(x.priority)} · ${fmt(x.created_at)}</div></div></div>`).join(''):'لا توجد تذاكر دعم.';}catch(e){root.querySelector('#saasTop').innerHTML=`<div class="card">${esc(e.message)}</div>`;}}
  function hook(){document.addEventListener('click',e=>{const b=e.target.closest('.nav button');if(b?.dataset.view==='control')setTimeout(()=>renderControl(document.getElementById('root')),0);});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',hook);else hook();
})();
