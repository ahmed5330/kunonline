/* kun online v4 modules — extends app-v3 without touching the production app */
let v4Suppliers=[],v4Finance=null,v4Transactions=[];
const v4StoreId=()=>activeClientId||state.businessClients?.[0]?.id||clientOf(state.orders?.[0])||'';
const v4Orders=()=>{const id=v4StoreId();return id?state.orders.filter(o=>String(clientOf(o))===String(id)):state.orders};
const v4Products=()=>{const id=v4StoreId();return id?state.products.filter(p=>String(clientOf(p))===String(id)):state.products};
const v4Pct=n=>`${Math.round((Number(n)||0)*10)/10}%`;
async function v4Api(url){const r=await fetch(url,{credentials:'include'});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'تعذر تحميل البيانات');return d}
async function suppliersV4(){root.innerHTML='<div class="card empty">جاري تحميل الموردين...</div>';try{const id=v4StoreId();v4Suppliers=await v4Api('/api/suppliers'+(id?`?clientId=${encodeURIComponent(id)}`:''));root.innerHTML=`<div class="page-head"><div><div class="title">الموردون والمشتريات</div><div class="sub">المرحلة الأولى من Procurement — الموردون ثم أوامر الشراء والاستلام</div></div><div class="spacer"></div><button class="btn primary" id="v4AddSupplier">+ مورد</button></div><div class="grid kpis four">${kpi('الموردون',money(v4Suppliers.length),'كل الموردين المسجلين للمتجر')}${kpi('النشطون',money(v4Suppliers.filter(s=>s.active!==0).length),'الموردون المتاحون للتوريد')}${kpi('Purchase Orders','قريبًا','أوامر الشراء ستُضاف في المرحلة التالية')}${kpi('Receiving','قريبًا','استلام المشتريات وربطها بالمخزون')}</div><div class="card table-wrap"><table class="table"><thead><tr><th>المورد</th><th>الهاتف</th><th>ملاحظات</th><th>الحالة</th><th>تاريخ الإضافة</th></tr></thead><tbody>${v4Suppliers.length?v4Suppliers.map(s=>`<tr><td><b>${esc(s.name)}</b></td><td>${esc(s.phone||'—')}</td><td>${esc(s.note||'—')}</td><td><span class="stock ${s.active===0?'danger':'ok'}">${s.active===0?'متوقف':'نشط'}</span></td><td>${esc((s.created_at||'').slice(0,10)||'—')}</td></tr>`).join(''):`<tr><td colspan="5" class="empty">لا يوجد موردون حتى الآن</td></tr>`}</tbody></table></div><div class="card v4-roadmap"><b>Procurement Roadmap:</b> Purchase Request → Purchase Order → Receiving → Supplier Invoice → Payment → Purchase Return.</div>`;v4Bind()}catch(e){root.innerHTML=`<div class="card empty">${esc(e.message)}</div>`}}
function shippingV4(){const o=v4Orders(),ship=o.filter(x=>x.awb||['shipped','signed','collected','returned'].includes(x.state)),del=ship.filter(x=>['signed','collected'].includes(x.state)).length,ret=ship.filter(x=>x.state==='returned').length,success=ship.length?del/ship.length*100:0;return `<div class="page-head"><div><div class="title">الشحن والتنفيذ</div><div class="sub">البوالص، التتبع، نجاح التسليم والمرتجعات</div></div></div><div class="grid kpis four">${kpi('الشحنات',money(ship.length),'طلبات لها بوليصة أو دخلت دورة الشحن')}${kpi('تم التسليم',money(del),'شحنات وصلت للعميل')}${kpi('نجاح التسليم',v4Pct(success),'المسلم ÷ إجمالي الشحنات')}${kpi('المرتجعات',money(ret),'شحنات رجعت للمتجر')}</div><div class="card table-wrap"><table class="table"><thead><tr><th>الطلب</th><th>العميل</th><th>البوليصة</th><th>الحالة</th><th>آخر تحديث</th><th>القيمة</th></tr></thead><tbody>${ship.length?ship.slice(0,150).map(x=>`<tr><td><b>${esc(x.id)}</b></td><td>${esc(x.name)}<div class="meta">${esc(x.phone)}</div></td><td>${esc(x.awb||'—')}</td><td>${badge(x.state)}</td><td>${esc(x.checkpoint||'—')}</td><td>${money(x.total)} EGP</td></tr>`).join(''):`<tr><td colspan="6" class="empty">لا توجد شحنات بعد</td></tr>`}</tbody></table></div>`}
async function financeV4(){root.innerHTML='<div class="card empty">جاري تحميل المالية والربحية...</div>';try{const id=v4StoreId(),q=id?`?clientId=${encodeURIComponent(id)}`:'';v4Finance=await v4Api('/api/finance'+q);v4Transactions=await v4Api('/api/transactions');const f=v4Finance||{},m=f.month||f.currentMonth||f,rev=Number(m.revenue??m.collected?.amount??0),profit=Number(m.profit??m.netProfit??0),ads=Number(m.adSpend??0),margin=Number(m.profitMarginPct??(rev?profit/rev*100:0));root.innerHTML=`<div class="page-head"><div><div class="title">المالية وProfit Intelligence</div><div class="sub">من الإيراد إلى الربح الحقيقي مع شرح كل رقم</div></div></div><div class="grid kpis">${kpi('الإيراد المحصل',money(rev)+' EGP','الإيراد/التحصيل الذي يحسبه محرك المالية الحالي')}${kpi('صافي الربح',money(profit)+' EGP','الربح بعد التكاليف المتاحة')}${kpi('هامش الربح',v4Pct(margin),'صافي الربح كنسبة من الإيراد')}${kpi('الإنفاق الإعلاني',money(ads)+' EGP','الإنفاق الإعلاني المسجل للفترة')}${kpi('الحركات المالية',money(v4Transactions.length),'المصاريف والإيرادات المسجلة')}</div><div class="grid split"><div class="card"><h3>معادلة الربحية ${help('المعادلة المستهدفة في النظام الكامل')}</h3><div class="v4-formula">Revenue − COGS − Shipping − Ads − Payment Fees − Expenses = Net Profit</div><p class="meta">الأرقام تتحسن دقتها كلما اكتملت بيانات تكلفة المنتج والشحن والإعلانات والمصاريف.</p></div><div class="card"><h3>آخر الحركات</h3>${v4Transactions.slice(0,10).map(t=>`<div class="v4-row"><span><b>${esc(t.category||t.type)}</b><small>${esc(t.date||'')}</small></span><span class="${t.type==='expense'?'v4-neg':'v4-pos'}">${t.type==='expense'?'-':'+'}${money(t.amount)} ${esc(t.currency||'EGP')}</span></div>`).join('')||'<div class="empty">لا توجد حركات</div>'}</div></div>`;v4Bind()}catch(e){root.innerHTML=`<div class="card empty">${esc(e.message)}</div>`}}
function marketingV4(){const o=v4Orders(),sources={};o.forEach(x=>{const s=x.source||'غير محدد';sources[s]=(sources[s]||0)+(+x.total||0)});const rows=Object.entries(sources).sort((a,b)=>b[1]-a[1]);return `<div class="page-head"><div><div class="title">التسويق والنمو</div><div class="sub">ربط المصدر بالطلب والإيراد تمهيدًا للـAttribution والربح الإعلاني</div></div></div><div class="grid split"><div class="card"><h3>المبيعات حسب المصدر</h3>${rows.map(([s,v])=>`<div class="v4-row"><span><b>${esc(s)}</b></span><span>${money(v)} EGP</span></div>`).join('')||'<div class="empty">لا توجد مصادر مبيعات</div>'}</div><div class="card"><h3>Attribution Architecture</h3><div class="v4-flow">Traffic → Session → Campaign → Customer → Order → Revenue → Profit</div><p class="meta">التالي: Meta + Google + TikTok، ثم First Touch / Last Touch / Multi Touch.</p></div></div>`}
function analyticsV4(){const o=v4Orders(),del=o.filter(x=>['signed','collected'].includes(x.state)).length,can=o.filter(x=>x.state==='cancelled').length,ret=o.filter(x=>x.state==='returned').length,total=o.reduce((s,x)=>s+(+x.total||0),0),aov=total/Math.max(o.length,1);return `<div class="page-head"><div><div class="title">التحليلات التنفيذية</div><div class="sub">Executive Analytics قابلة لاحقًا للـDrill-down حتى مستوى الطلب</div></div></div><div class="grid kpis four">${kpi('AOV',money(aov)+' EGP','متوسط قيمة الطلب')}${kpi('Delivery Rate',v4Pct(o.length?del/o.length*100:0),'نسبة الطلبات المسلمة')}${kpi('Cancellation Rate',v4Pct(o.length?can/o.length*100:0),'نسبة الإلغاء')}${kpi('Return Rate',v4Pct(o.length?ret/o.length*100:0),'نسبة المرتجع')}</div><div class="card"><h3>Drill-down</h3><div class="v4-flow">Company → Store → Channel → Campaign → Product → Order</div><p class="meta">الهدف: كل رقم في الداشبورد يصبح قابلًا للنقر لمعرفة السبب وراءه.</p></div>`}
function automationV4(){return `<div class="page-head"><div><div class="title">الأتمتة</div><div class="sub">Workflow Engine — حدث، شروط، إجراءات، تأخير وفروع</div></div><div class="spacer"></div><button class="btn primary" id="v4NewFlow">+ Workflow</button></div><div class="grid v4-three"><div class="card"><h3>1. Trigger</h3><p>Order Created</p><p>Order Delivered</p><p>Low Stock</p><p>Customer Inactive</p></div><div class="card"><h3>2. Conditions</h3><p>Order Value</p><p>Customer Segment</p><p>Product / Channel</p><p>Risk Score</p></div><div class="card"><h3>3. Actions</h3><p>Send WhatsApp</p><p>Assign Agent</p><p>Add Tag</p><p>Notify Team</p></div></div><div class="card v4-roadmap"><b>قاعدة الأمان:</b> أي إجراء مالي أو حساس سيحتاج Permission + Confirmation + Audit Log.</div>`}
function aiV4(){const o=v4Orders(),p=v4Products(),pend=o.filter(x=>x.state==='pending').length,low=p.filter(isLow).length,ret=o.filter(x=>x.state==='returned').length;return `<div class="page-head"><div><div class="title">✨ kun AI</div><div class="sub">Explain → Recommend → Act → Automate</div></div></div><div class="grid split"><div class="card"><h3>توصيات من بياناتك</h3>${pend?`<div class="insight warn"><b>${pend} طلب جديد يحتاج متابعة</b><div class="meta">تقليل زمن التأكيد قد يحسن التسليم.</div></div>`:''}${low?`<div class="insight danger"><b>${low} منتج منخفض المخزون</b><div class="meta">ابدأ بالأسرع حركة ثم راجع الموردين.</div></div>`:''}${ret?`<div class="insight info"><b>${ret} طلب مرتجع</b><div class="meta">حلل المنتج والمنطقة وشركة الشحن.</div></div>`:''}${!pend&&!low&&!ret?'<div class="insight ok"><b>لا توجد مشاكل حرجة الآن</b></div>':''}</div><div class="card"><h3>اسأل kun AI</h3><input class="input" id="v4AiQuestion" placeholder="مثال: ليه المرتجعات زادت؟"><button class="btn primary v4-full" id="v4AskAi">تحليل</button><p class="meta">حاليًا Insight Engine من بيانات المتجر. ربط LLM سيتم بعد تثبيت Multi-Tenant AI permissions.</p></div></div>`}
const renderV3=render,bindV3=bind;
render=function(){if(view==='suppliers')return suppliersV4();if(view==='shipping'){root.innerHTML=shippingV4();return v4Bind()}if(view==='finance')return financeV4();if(view==='marketing'){root.innerHTML=marketingV4();return v4Bind()}if(view==='analytics'){root.innerHTML=analyticsV4();return v4Bind()}if(view==='automation'){root.innerHTML=automationV4();return v4Bind()}if(view==='ai'){root.innerHTML=aiV4();return v4Bind()}return renderV3()};
function v4Bind(){bindV3();document.getElementById('v4AddSupplier')?.addEventListener('click',()=>toast('نموذج إضافة المورد سيتم ربطه بـ POST /api/suppliers'));document.getElementById('v4NewFlow')?.addEventListener('click',()=>toast('Workflow Builder هو المرحلة التالية للأتمتة'));document.getElementById('v4AskAi')?.addEventListener('click',()=>toast('سيتم ربط AI Copilot بعد تثبيت طبقة البيانات والصلاحيات'))}
bind=function(){v4Bind()};
