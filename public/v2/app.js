const root=document.getElementById('root'),drawer=document.getElementById('drawer'),drawerBack=document.getElementById('drawerBack');let state={orders:[],clients:[],products:[]},view='dashboard',status='all';const S={pending:'جديد',confirmed:'مؤكد',preparing:'تجهيز',shipped:'تم الشحن',delivered:'تم التسليم',cancelled:'ملغي',returned:'مرتجع',deferred:'مؤجل'};function esc(v=''){return String(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function money(v){return new Intl.NumberFormat('ar-EG',{maximumFractionDigits:0}).format(Number(v)||0)}function help(t){return `<span class="help" title="${esc(t)}">i</span>`}function toast(t){const e=document.getElementById('toast');e.textContent=t;e.style.display='block';setTimeout(()=>e.style.display='none',2200)}async function load(){try{const r=await fetch('/api/state',{credentials:'include'});if(!r.ok)throw new Error('سجّل الدخول أولًا من النظام الحالي');const d=await r.json();state.orders=d.orders||[];state.clients=d.clients||[];state.products=d.products||[];render()}catch(e){root.innerHTML=`<div class="card empty"><h2>تعذر تحميل البيانات</h2><p>${esc(e.message)}</p><a class="btn primary" href="/">العودة لتسجيل الدخول</a></div>`}}function filtered(){let arr=[...state.orders];if(status!=='all')arr=arr.filter(o=>o.state===status);const q=(document.getElementById('orderSearch')?.value||'').trim().toLowerCase();if(q)arr=arr.filter(o=>[o.id,o.name,o.phone,o.awb].some(x=>String(x||'').toLowerCase().includes(q)));return arr}function kpi(label,val,tip){return `<div class="card"><div class="k-label">${label} ${help(tip)}</div><div class="k-val">${val}</div></div>`}function dashboard(){const o=state.orders,total=o.reduce((s,x)=>s+(Number(x.total)||0),0),del=o.filter(x=>x.state==='delivered').length,pend=o.filter(x=>x.state==='pending').length,ship=o.filter(x=>x.state==='shipped').length;return `<div class="page-head"><div><div class="title">صباح الخير 👋</div><div class="sub">إليك ملخص أداء التجارة الإلكترونية من مكان واحد</div></div><div class="spacer"></div><button class="btn primary" data-go="orders">عرض الطلبات</button></div><div class="grid kpis">${kpi('إجمالي المبيعات',money(total)+' EGP','إجمالي قيمة الطلبات الموجودة في النظام')}${kpi('عدد الطلبات',money(o.length),'كل الطلبات المسجلة')}${kpi('تم التسليم',money(del),'الطلبات التي وصلت للعميل')}${kpi('طلبات جديدة',money(pend),'طلبات تحتاج متابعة أو تأكيد')}${kpi('قيد الشحن',money(ship),'طلبات خرجت لشركة الشحن')}</div><div class="grid" style="grid-template-columns:1.4fr .8fr"><div class="card"><h3>أهم التنبيهات</h3><p>✨ kun AI: يوجد ${pend} طلب جديد يحتاج متابعة.</p><p>📦 يوجد ${ship} طلب قيد الشحن.</p><p>👥 قاعدة العملاء مرتبطة مباشرة بالطلبات لتكوين Customer 360.</p></div><div class="card"><h3>سهولة الاستخدام</h3><p>علامة ${help('اضغط على علامة i بجانب أي رقم لفهم معناه')} موجودة بجانب المؤشرات المهمة.</p><p>كل الأرقام قابلة للتحول لاحقًا إلى Drill-down تفصيلي.</p></div></div>`}function badge(st){return `<span class="badge b-${st}">${S[st]||esc(st||'—')}</span>`}function orders(){const arr=filtered();const statuses=['all','pending','confirmed','preparing','shipped','delivered','returned','cancelled'];return `<div class="page-head"><div><div class="title">الطلبات</div><div class="sub">إدارة دورة الطلب من الاستلام حتى التسليم وما بعد البيع</div></div><div class="spacer"></div><button class="btn primary" id="newOrder">+ طلب جديد</button></div><div class="tabs">${statuses.map(s=>`<button class="tab ${status===s?'active':''}" data-status="${s}">${s==='all'?'الكل':S[s]||s}</button>`).join('')}</div><div class="toolbar"><input class="input" id="orderSearch" placeholder="بحث برقم الطلب، العميل، الهاتف، البوليصة"><select class="select"><option>كل قنوات البيع</option></select><select class="select"><option>كل طرق الدفع</option></select><select class="select"><option>كل شركات الشحن</option></select><button class="btn soft" id="saveView">حفظ العرض</button></div><div class="card table-wrap"><table class="table"><thead><tr><th># الطلب</th><th>العميل</th><th>الهاتف</th><th>القيمة</th><th>الحالة ${help('حالة الطلب الحالية في دورة التشغيل')}</th><th>الشحن</th><th>إجراء</th></tr></thead><tbody>${arr.length?arr.map(o=>`<tr><td><b>${esc(o.id)}</b></td><td>${esc(o.name)}</td><td>${esc(o.phone)}</td><td>${money(o.total)} EGP</td><td>${badge(o.state)}</td><td>${esc(o.awb||'—')}</td><td><button class="btn soft" data-order="${esc(o.id)}">فتح</button></td></tr>`).join(''):`<tr><td colspan="7" class="empty">لا توجد طلبات مطابقة</td></tr>`}</tbody></table></div>`}function placeholder(name){return `<div class="page-head"><div><div class="title">${name}</div><div class="sub">سيتم تنفيذ هذا الموديول على نفس الـDesign System بعد تثبيت الطلبات وCustomer 360.</div></div></div><div class="card empty">الهيكل جاهز للربط التدريجي بالـAPI الحالي بدون كسر النظام الموجود.</div>`}function render(){root.innerHTML=view==='dashboard'?dashboard():view==='orders'?orders():placeholder(({customers:'العملاء CRM',products:'المنتجات',inventory:'المخزون',marketing:'التسويق',finance:'المالية',analytics:'التحليلات',ai:'kun AI',settings:'الإعدادات'})[view]||view);bind()}function bind(){document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>setView(b.dataset.go));document.querySelectorAll('[data-status]').forEach(b=>b.onclick=()=>{status=b.dataset.status;render()});const q=document.getElementById('orderSearch');if(q)q.oninput=()=>renderOrdersBody();document.querySelectorAll('[data-order]').forEach(b=>b.onclick=()=>openOrder(b.dataset.order));document.getElementById('saveView')?.addEventListener('click',()=>toast('تم حفظ العرض محليًا كنموذج UX'));document.getElementById('newOrder')?.addEventListener('click',()=>toast('سيتم ربط إنشاء الطلب بنموذج النظام الحالي'))}function renderOrdersBody(){const body=document.querySelector('.table tbody');if(!body)return;const arr=filtered();body.innerHTML=arr.length?arr.map(o=>`<tr><td><b>${esc(o.id)}</b></td><td>${esc(o.name)}</td><td>${esc(o.phone)}</td><td>${money(o.total)} EGP</td><td>${badge(o.state)}</td><td>${esc(o.awb||'—')}</td><td><button class="btn soft" data-order="${esc(o.id)}">فتح</button></td></tr>`).join(''):`<tr><td colspan="7" class="empty">لا توجد طلبات مطابقة</td></tr>`;document.querySelectorAll('[data-order]').forEach(b=>b.onclick=()=>openOrder(b.dataset.order))}function openOrder(id){const o=state.orders.find(x=>String(x.id)===String(id));if(!o)return;drawer.innerHTML=`<div class="page-head"><div><div class="title">${esc(o.id)}</div><div class="sub">تفاصيل الطلب وTimeline</div></div><div class="spacer"></div><button class="btn soft" id="closeDrawer">إغلاق</button></div><div class="card"><h3>${esc(o.name)}</h3><p>${esc(o.phone)}</p><p>القيمة: <b>${money(o.total)} EGP</b></p><p>الحالة: ${badge(o.state)}</p><p>البوليصة: ${esc(o.awb||'غير متاحة')}</p></div><div class="card" style="margin-top:12px"><h3>رحلة الطلب</h3><div class="timeline"><div class="tl"><span class="dot"></span><div><b>إنشاء الطلب</b><div class="meta">تم تسجيل الطلب في النظام</div></div></div><div class="tl"><span class="dot"></span><div><b>الحالة الحالية</b><div class="meta">${S[o.state]||o.state}</div></div></div></div></div><div class="card" style="margin-top:12px"><h3>الربحية</h3><p class="meta">سيتم ربط تكلفة المنتج والشحن والإعلان لإظهار Contribution Profit الحقيقي لكل طلب.</p></div>`;drawer.classList.add('open');drawerBack.classList.add('show');document.getElementById('closeDrawer').onclick=closeDrawer}function closeDrawer(){drawer.classList.remove('open');drawerBack.classList.remove('show')}function setView(v){view=v;document.querySelectorAll('.nav button').forEach(b=>b.classList.toggle('active',b.dataset.view===v));render()}document.querySelectorAll('.nav button').forEach(b=>b.onclick=()=>setView(b.dataset.view));drawerBack.onclick=closeDrawer;document.getElementById('quickBtn').onclick=()=>toast('Quick Create: طلب، عميل، منتج، مصروف، حملة');load();