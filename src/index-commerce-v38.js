import commerceV37 from './index-commerce-v37.js';
import {requirePermission} from './access-control.js';
import {readConnectionSecrets} from './integration-provider-validation.js';
import {jtCredentials,__jtApiInternals} from './jt-express-eg-api.js';

const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'}});
const clean=(value,max=1000)=>String(value??'').trim().slice(0,max);
async function currentUser(request,env,ctx){
  const hasAuth=Boolean(clean(request.headers.get('Cookie'))||clean(request.headers.get('Authorization')));
  if(!hasAuth)throw Object.assign(new Error('محتاج تسجّل دخول'),{status:401,code:'AUTH_REQUIRED'});
  const url=new URL(request.url);url.pathname='/api/me';url.search='';
  const response=await commerceV37.fetch(new Request(url,{method:'GET',headers:request.headers}),env,ctx),data=await response.json().catch(()=>({}));
  if(!response.ok||!data?.role){
    const explicitAuthFailure=data?.code==='AUTH_REQUIRED'||/محتاج\s*تسج[ّ]?ل\s*دخول/i.test(String(data?.error||''));
    const status=explicitAuthFailure?401:(!response.ok&&response.status>=400?response.status:401);
    throw Object.assign(new Error(data?.error||'محتاج تسجّل دخول'),{status,code:'AUTH_REQUIRED'});
  }
  return data;
}
function clientIdFor(me,request,body={}){const url=new URL(request.url),requested=clean(body.clientId||body.client_id||url.searchParams.get('clientId')||me?.clientId,160);if(me.role==='client'){if(requested&&String(requested)!==String(me.clientId))throw Object.assign(new Error('مش مسموح الوصول لبيانات متجر آخر'),{status:403});return clean(me.clientId,160);}if(!requested)throw Object.assign(new Error('محتاج clientId'),{status:400});return requested;}
async function connectionFor(env,clientId){const row=await env.DB.prepare("SELECT * FROM store_connections WHERE client_id=? AND provider='jt' ORDER BY CASE status WHEN 'connected' THEN 0 WHEN 'configured' THEN 1 ELSE 2 END,updated_at DESC,created_at DESC LIMIT 1").bind(clientId).first();if(!row)throw Object.assign(new Error('لا يوجد ربط J&T لهذا العميل'),{status:404});return {row,secrets:await readConnectionSecrets(env,clientId,row.id)};}
function authFailure(text=''){return /(auth|credential|customer\s*code|customer\s*password|customer\s*pwd|digest|signature|sign|account|password|unauthor|forbidden|密钥|签名|密码|客户)/i.test(text);}
async function attempt(label,payload,secrets){try{const result=await __jtApiInternals.signedPost(__jtApiInternals.GET_ORDERS_PATH,payload,secrets,{fetcher:fetch}),text=`${result.code} ${result.message}`;return {label,httpStatus:result.response.status,code:result.code||null,message:result.message||null,success:__jtApiInternals.success(result),credentialLikelyAccepted:__jtApiInternals.success(result)||(result.response.ok&&!authFailure(text))};}catch(error){return {label,httpStatus:error?.status||0,code:error?.code||null,message:clean(error?.message,600),success:false,credentialLikelyAccepted:false};}}
async function diagnose(request,env,ctx){if(env.APP_ENV!=='preview')return json({error:'Not found'},404);const me=await currentUser(request,env,ctx);requirePermission(me,'integrations','read');const body=await request.clone().json().catch(()=>({})),clientId=clientIdFor(me,request,body),{row,secrets}=await connectionFor(env,clientId),{fields}=jtCredentials(secrets),serial=`KUN-AUTH-PROBE-${Date.now()}`,base={sourceCode:fields.sourceCode,command:1,serialNumber:[serial]},variants=[{label:'developer-info-only',payload:base}];
  if(fields.customerCode&&fields.customerPassword)variants.push({label:'saved-enterprise-info',payload:{...base,customerCode:fields.customerCode,digest:__jtApiInternals.businessDigest(fields.customerCode,fields.customerPassword,fields.privateKey)}});
  const legacy=clean(secrets.api_key,500);if(legacy){variants.push({label:'legacy-api-key-as-business-digest',payload:{...base,customerCode:fields.sourceCode,digest:legacy}});variants.push({label:'source-code-plus-legacy-api-key-password',payload:{...base,customerCode:fields.sourceCode,digest:__jtApiInternals.businessDigest(fields.sourceCode,legacy,fields.privateKey)}});variants.push({label:'api-account-plus-legacy-api-key-password',payload:{...base,customerCode:fields.apiAccount,digest:__jtApiInternals.businessDigest(fields.apiAccount,legacy,fields.privateKey)}});}
  const attempts=[];for(const variant of variants)attempts.push(await attempt(variant.label,variant.payload,secrets));return json({ok:true,clientId,connectionId:row.id,connectionStatus:row.status,has:{apiAccount:Boolean(fields.apiAccount),privateKey:Boolean(fields.privateKey),sourceCode:Boolean(fields.sourceCode),customerCode:Boolean(fields.customerCode),customerPassword:Boolean(fields.customerPassword),legacyApiKey:Boolean(legacy)},attempts});}
async function requireCreateBusinessCredentials(request,env,me){const body=await request.clone().json().catch(()=>({})),clientId=clientIdFor(me,request,body),{secrets}=await connectionFor(env,clientId),{fields}=jtCredentials(secrets),missing=[];if(!fields.customerCode)missing.push('Customer Code');if(!fields.customerPassword)missing.push('Customer Password');if(missing.length)throw Object.assign(new Error(`قبل إنشاء شحنة J&T لازم تحفظ ${missing.join(' و ')} في مركز التكاملات. استخدم القيم التي سلّمتها لك J&T لحساب الشحن؛ لا تستخدم Private Key بدل Customer Password.`),{status:409,code:'JT_BUSINESS_CREDENTIALS_MISSING',missingBusinessFields:missing});}
function jtErrorResponse(error){const raw=Number(error?.status),status=Number.isInteger(raw)&&raw>=400&&raw<=599?raw:500;return json({error:error?.message||'حدث خطأ في تكامل J&T',code:error?.code||'JT_ERROR',jtCode:error?.jtCode||null,enterpriseCredentialsRequired:Boolean(error?.enterpriseCredentialsRequired),missingSenderFields:Array.isArray(error?.missingSenderFields)?error.missingSenderFields:undefined,missingBusinessFields:Array.isArray(error?.missingBusinessFields)?error.missingBusinessFields:undefined},status);}
async function fetchV38(request,env,ctx){const url=new URL(request.url),isJtApi=url.pathname.startsWith('/api/jt/'),isJtWebhook=url.pathname.startsWith('/api/webhooks/jt/'),isJt=isJtApi||isJtWebhook;try{let me=null;if(isJtApi)me=await currentUser(request,env,ctx);if(url.pathname==='/api/jt/diagnostic'&&request.method==='POST')return await diagnose(request,env,ctx);if(request.method==='POST'&&/^\/api\/jt\/shipments\/[^/]+$/.test(url.pathname))await requireCreateBusinessCredentials(request,env,me);const response=await commerceV37.fetch(request,env,ctx);if(isJt&&response.status===200){const data=await response.clone().json().catch(()=>null);if(data?.code==='AUTH_REQUIRED')return json(data,401);}return response;}catch(error){if(isJt)return jtErrorResponse(error);throw error;}}
export {SyncEntrypoint} from './index-commerce-v37.js';
export default {fetch:fetchV38,scheduled(controller,env,ctx){return commerceV37.scheduled?.(controller,env,ctx);}};
