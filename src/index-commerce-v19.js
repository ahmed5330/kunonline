import commerceV18 from './index-commerce-v18.js';
import {requirePermission} from './access-control.js';
import {providerById} from './provider-registry.js';
const json=(d,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{'Content-Type':'application/json; charset=utf-8'}});
async function me(request,env,ctx){const u=new URL(request.url);u.pathname='/api/me';u.search='';const r=await commerceV18.fetch(new Request(u,{method:'GET',headers:request.headers}),env,ctx),d=await r.json().catch(()=>({}));if(!r.ok||!d?.role)throw Object.assign(new Error(d?.error||'محتاج تسجّل دخول'),{status:r.status||401});return d;}
function tenant(m,r){if(m.role==='client'){if(r&&String(r)!==String(m.clientId))throw Object.assign(new Error('مش مسموح الوصول لبيانات متجر آخر'),{status:403,code:'TENANT_ISOLATION'});return m.clientId;}if(!r)throw Object.assign(new Error('محتاج clientId'),{status:400,code:'CLIENT_ID_REQUIRED'});return r;}
async function fetchV19(request,env,ctx){const u=new URL(request.url),p=u.pathname,method=request.method.toUpperCase(),x=p.match(/^\/api\/integrations\/connections\/([^/]+)\/validate$/);try{if(!x||method!=='POST')return commerceV18.fetch(request,env,ctx);const m=await me(request,env,ctx);requirePermission(m,'integrations','write');const b=await request.json().catch(()=>({}));const clientId=tenant(m,b.clientId||u.searchParams.get('clientId')||(m.role==='client'?m.clientId:null)),connectionId=decodeURIComponent(x[1]);const c=await env.DB.prepare('SELECT id,provider FROM store_connections WHERE id=? AND client_id=?').bind(connectionId,clientId).first();if(!c)return json({error:'التكامل غير موجود'},404);const provider=providerById(c.provider);if(!provider)return json({error:'Provider غير مدعوم'},400);const {results=[]}=await env.DB.prepare('SELECT secret_name FROM integration_secrets WHERE client_id=? AND connection_id=?').bind(clientId,connectionId).all();const names=new Set(results.map(r=>r.secret_name)),missing=provider.requiredSecrets.filter(s=>!names.has(s));const status=missing.length?'disconnected':'configured';await env.DB.prepare('UPDATE store_connections SET status=?,last_error=?,updated_at=? WHERE id=? AND client_id=?').bind(status,missing.length?`Missing credentials: ${missing.join(', ')}`:null,new Date().toISOString(),connectionId,clientId).run();return json({ok:!missing.length,status,missingSecrets:missing,externalConnectivityChecked:false});}catch(e){return json({error:e.message||'حدث خطأ',code:e.code||null},e.status||500);}}
export default {fetch:fetchV19,scheduled(c,e,x){return commerceV18.scheduled?.(c,e,x);}};
